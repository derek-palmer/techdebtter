/**
 * CLI entrypoint placeholder for Task 1 packaging.
 * Commands land in later tracer slices once analyze/publish exist.
 */
process.stderr.write(
  "techdebtter: CLI commands are not implemented yet. Use the library exports for domain contracts.\n",
);
process.exitCode = 1;
